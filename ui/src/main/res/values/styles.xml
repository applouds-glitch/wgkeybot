<?xml version="1.0" encoding="utf-8"?><!--
  ~ Copyright © 2017-2025 WireGuard LLC. All Rights Reserved.
  ~ SPDX-License-Identifier: Apache-2.0
  -->
<resources xmlns:android="http://schemas.android.com/apk/res/android">

    <style name="WireGuardTheme.Toolbar" parent="Widget.Material3.Toolbar">
        <item name="android:background">@color/wgk_top_bar</item>
    </style>

    <style name="AppThemeBase" parent="WireGuardTheme">
        <item name="materialCardViewStyle">@style/WireGuardTheme.MaterialCardView</item>
        <item name="toolbarStyle">@style/WireGuardTheme.Toolbar</item>
        <item name="bottomSheetDialogTheme">@style/WireGuardTheme.BottomSheetDialog</item>
        <item name="android:statusBarColor">@color/wgk_top_bar</item>
        <item name="android:windowLightStatusBar">@bool/light_status_bar</item>
    </style>

    <!-- Various additional API-specific features in values-v*/styles.xml -->
    <style name="AppTheme" parent="AppThemeBase" />

    <!-- TV variant: hard-coded dark window background so there is no white flash
         on startup regardless of the device's current night-mode setting. -->
    <style name="AppTheme.Tv" parent="AppTheme">
        <item name="android:windowBackground">@color/wgk_tv_bg_splash</item>
    </style>

    <style name="WireGuardTheme.MaterialCardView" parent="Widget.Material3.CardView.Elevated">
        <item name="cornerRadius">4dp</item>
        <item name="contentPadding">8dp</item>
    </style>

    <style name="WireGuardTheme.BottomSheetDialog" parent="ThemeOverlay.Material3.BottomSheetDialog">
        <item name="android:windowIsFloating">false</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
    </style>
    <style name="TvKeyButton" parent="Widget.Material3.Button">
        <item name="android:layout_width">0dp</item>
        <item name="android:layout_height">52dp</item>
        <item name="android:layout_weight">1</item>
        <item name="android:layout_marginHorizontal">3dp</item>
        <item name="android:focusable">true</item>
        <item name="android:textSize">18sp</item>
        <item name="android:textStyle">bold</item>
        <item name="android:fontFamily">monospace</item>
        <item name="cornerRadius">8dp</item>
        <!-- Focused key inverts (white fill, primary text) and gains a thick
             primary border so it's instantly identifiable from across the room. -->
        <item name="backgroundTint">@color/wgk_tv_key_bg</item>
        <item name="android:textColor">@color/wgk_tv_key_text</item>
        <item name="strokeColor">@color/wgk_tv_key_stroke</item>
        <item name="strokeWidth">2dp</item>
    </style>

    <style name="TvKeyButton.Action" parent="TvKeyButton">
        <item name="android:textSize">14sp</item>
        <item name="android:textStyle">normal</item>
        <item name="android:fontFamily">sans-serif</item>
        <item name="backgroundTint">@color/wgk_tv_key_action_bg</item>
        <item name="android:textColor">@color/wgk_tv_key_action_text</item>
    </style>

    <style name="TvKeyButton.Connect" parent="TvKeyButton">
        <item name="android:textSize">14sp</item>
        <item name="android:textStyle">normal</item>
        <item name="android:fontFamily">sans-serif</item>
        <!-- Inherits backgroundTint/textColor from TvKeyButton so the focus
             inversion applies identically to the Connect key. -->
    </style>

    <style name="Widget.WGKeyBot.ConnectButton.Tv" parent="Widget.WGKeyBot.ConnectButton">
        <item name="strokeColor">@color/wgk_tv_connect_stroke</item>
        <item name="strokeWidth">5dp</item>
    </style>

    <style name="NoBackgroundTheme" parent="AppTheme">
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowContentOverlay">@null</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowActionBar">false</item>
        <item name="android:windowAnimationStyle">@null</item>
        <item name="android:backgroundDimEnabled">false</item>
        <item name="android:windowDisablePreview">true</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:windowDrawsSystemBarBackgrounds">true</item>
    </style>

</resources>
